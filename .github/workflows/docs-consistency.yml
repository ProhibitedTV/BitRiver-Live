name: Docs consistency

on:
  push:
    paths:
      - 'README.md'
      - 'docs/**/*.md'
      - 'deploy/.env.example'
      - 'scripts/check-doc-installer-language.sh'
      - 'scripts/generate-contract-doc.sh'
      - '.github/workflows/docs-consistency.yml'
  pull_request:
    paths:
      - 'README.md'
      - 'docs/**/*.md'
      - 'deploy/.env.example'
      - 'scripts/check-doc-installer-language.sh'
      - 'scripts/generate-contract-doc.sh'
      - '.github/workflows/docs-consistency.yml'

jobs:
  installer-wording:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check installer milestone wording
        run: ./scripts/check-doc-installer-language.sh

      - name: Check generated contract env docs are fresh
        run: ./scripts/generate-contract-doc.sh --check
