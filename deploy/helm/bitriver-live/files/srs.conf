# GENERATED FILE: DO NOT EDIT DIRECTLY
# Canonical source: deploy/srs/conf/srs.conf
# Regenerate with: ./scripts/sync-helm-deploy-assets.sh

listen              1935;
max_connections     1000;
daemon              off;

srs_log_tank        console;

http_api {
    enabled         on;
    listen          1985;
    # Keep the raw API disabled by default; only enable it when you explicitly
    # publish the SRS API port for debugging in a trusted environment.
    raw_api {
        enabled     off;
    }
}

http_server {
    enabled         on;
    listen          8080;
    dir             ./objs/nginx/html;
}

vhost __defaultVhost__ {
    tcp_nodelay     on;
    min_latency     on;

    http_remux {
        enabled     on;
        mount       [vhost]/[app]/[stream].flv;
        hstrs       on;
    }

    # Notify the BitRiver Live ingest hooks whenever publishers or viewers
    # connect so the API can validate stream keys and tear down sessions.
    # Populate BITRIVER_SRS_TOKEN in your .env and adjust the host if the API
    # is running elsewhere.
    http_hooks {
        enabled         on;
        on_connect      http://bitriver-live:8080/api/ingest/srs/connect?token=${BITRIVER_SRS_TOKEN};
        on_publish      http://bitriver-live:8080/api/ingest/srs/publish?token=${BITRIVER_SRS_TOKEN};
        on_unpublish    http://bitriver-live:8080/api/ingest/srs/unpublish?token=${BITRIVER_SRS_TOKEN};
        on_play         http://bitriver-live:8080/api/ingest/srs/play?token=${BITRIVER_SRS_TOKEN};
        on_stop         http://bitriver-live:8080/api/ingest/srs/stop?token=${BITRIVER_SRS_TOKEN};
    }

    # Forward only streams accepted by the shared-token hook into the static
    # OME live application. The control plane maps the private publish key to
    # the public channel ID before returning the destination URL.
    forward {
        enabled         on;
        backend         http://bitriver-live:8080/api/ingest/srs/forward?token=${BITRIVER_SRS_TOKEN};
    }
}
