apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bitriver-live.fullname" . }}-srs-conf
  labels:
    {{- include "bitriver-live.labels" . | nindent 4 }}
data:
  srs.conf: |-
{{ .Files.Get "files/srs.conf" | replace "${BITRIVER_SRS_TOKEN}" .Values.secrets.BITRIVER_SRS_TOKEN | indent 4 }}
