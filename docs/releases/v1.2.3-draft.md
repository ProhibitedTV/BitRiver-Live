# BitRiver Live v1.2.3 draft release notes

Status: stable-line draft. Public prerelease
[`v1.2.3-rc.22`](https://github.com/ProhibitedTV/BitRiver-Live/releases/tag/v1.2.3-rc.22)
remains available as historical qualification evidence, but is not eligible
for stable promotion after a production-image scan identified critical
`CVE-2026-56854` in its `golang.org/x/crypto v0.54.0` dependency. Source now
requires patched `v0.55.0`; a new immutable candidate must publish and rerun
the release and external gates before stable `v1.2.3` promotion.
RC22 carries release-set root
`938b22cc5581275bf6788f2144367e49626eff0964586485ba303d339218a2b8`
and the accepted exact-package recovered-stack golden-path proof plus the
service-resilience/capacity foundations required by remaining qualification;
those results remain historical RC22 evidence only. All eight external gates
must rerun and bind to the patched successor candidate's exact root hash before
stable promotion.

## Summary

BitRiver Live packages the core pieces of a self-hosted live-streaming stack into one repository and one deployment contract:

- Go API and admin control plane
- Next.js viewer
- SRS ingest
- OvenMediaEngine playback
- FFmpeg-based transcoding
- Postgres and Redis
- Compose-based deployment assets, installers, and release automation

This release focuses on making that stack approachable and trustworthy for public adopters: cleaner repo hygiene, clearer onboarding, explicit support boundaries, and a more complete contribution and release surface.

## What operators get

- A documented single-host deployment baseline centered on `deploy/docker-compose.yml`
- Quickstart and smoke-test flows from the same source tree
- A production golden-path gate that publishes deterministic 1080p RTMP,
  decodes OME and transcoder playback, exercises chat/moderation and VOD, checks
  aggregate health, scans its evidence, and tears down
- Source-free Linux launcher archives and amd64/arm64 `.deb`/`.rpm` packages assembled from one canonical asset manifest
- An Ubuntu systemd host manager that stages the Compose stack under `/opt/bitriver-live`, keeps configuration/data separate, and never activates sample credentials
- Packaged Postgres plus encrypted host-state recovery commands that bind
  configuration, local media, database evidence, and optional object inventory
  to exact release identity without retaining plaintext archives or secrets in
  reports
- An isolated service-restart rehearsal that injects API, Postgres, Redis,
  SRS/controller, OvenMediaEngine, transcoder, and viewer outages while proving
  bounded recovery, preserved session/channel identity, stable restart counts,
  secret-safe reporting, and complete teardown
- Release artifacts for binaries, Cosign-bundled launcher binaries, viewer
  bundle, and version-matched container images, including the OME config helper
- Candidate-only builds with a signed manifest covering every public artifact,
  five exact image digests/signatures, SBOMs, dependency pins, and sanitized
  contract/product/scan evidence
- A protected, environment-reviewed stable workflow that copies candidate
  bytes, retags exact digests, publishes through a draft, emits signed rollback
  metadata, and supports append-only candidate revocation without rebuilding
- Five first-party images under `ghcr.io/prohibitedtv`, published under the
  exact candidate tag and checked for anonymous access before the GitHub
  prerelease is created
- Supporting runbooks for security, upgrades, monitoring, and production release execution

## Security status

- Production builds require `golang.org/x/crypto v0.55.0`, which fixes critical
  `CVE-2026-56854` reported by the blocking image scanner. BitRiver imports the
  module's PBKDF2 package rather than the affected SSH surface, but release
  policy does not waive unresolved critical dependency findings.
- Immutable RC22 still contains `v0.54.0` and is therefore rejected for stable
  promotion. The next candidate must pass the blocking production-image scan;
  this source change alone is not candidate evidence.

## Known limits

- The supported baseline is still operator-managed single-host deployment.
- HA or multi-host guarantees are not part of this release.
- Kubernetes is not the primary deployment path in this repository.
- Operators still own host hardening, secret rotation, backups, and capacity planning.
- Ubuntu Server 24.04 LTS on amd64 is the production installation target. RC22 passed hosted no-checkout Ubuntu package install, activation, same-tag upgrade, OME/Docker/systemd restart, and uninstall-retention qualification. Debian 12, RPM hosts, arm64, and physical XOA/NPM reboot/media recovery remain provisional.
- Source-built canonical Compose has automated real RTMP, OME/transcoder
  decode, chat/moderation, VOD, and aggregate-health proof. The tag workflow
  repeats that gate from the five published, digest-pinned first-party images.
  RC22's hosted clean-host run covers package installation, authenticated OME
  control, service/daemon restart, and uninstall retention. Physical Nginx
  Proxy Manager playback, host reboot, router/firewall policy, real media after
  reboot, and browser recovery/quality behavior still require target evidence
  before stable promotion.

## Recommended upgrade / rollout notes

1. Validate your target environment with `deploy/check-env.sh`.
2. Follow [`docs/production-release.md`](../production-release.md) before tagging or rolling out.
3. Confirm release notes, checksums, image digests, and the changelog agree on
   the exact immutable tag. Never move a failed RC tag; fix the source and
   increment the candidate number.
4. Run `scripts/test-production-golden-path.sh --stack running --client docker`
   against the deliberately prepared release-candidate installation and retain
   its sanitized JSON report.
5. Run `scripts/test-stateful-upgrade.sh` and
   `scripts/test-stateful-compose-upgrade.sh`, retaining both secret-scanned
   reports. The current RC19-to-RC20 result proves database/migration-layer
   in-place compatibility, the exact-image Compose/config rollback, a
   migration/config interruption cut, and the post-upgrade production golden
   path. RC19 remains rejected; this does not prove an approved rollback root.
6. Retain RC22's sanitized published-package recovered-stack report
   `15aba647f1b0049bb961715a822cea047c651a172a41620965520be233e19622`.
   Both original and runtime Postgres restores used disposable read-only
   helpers bound to the exact release-set Postgres image; all 13 digest-pinned
   services and eight golden-path stages passed, four fixture users and one
   representative fixture survived, and two new users persisted. Observed RPO
   was 24s, restore-only RTO 22s, and total product RTO 110s. RC21's earlier
   mutable-container report remains rejected; only production-like scheduled/
   off-host backup freshness and RPO evidence remains for this recovery gate.
7. Run `scripts/test-service-resilience.sh` and retain its
   `bitriver.service-resilience/v1` report as local-build evidence. Repeat the
   seven service failures from the exact next candidate on the clean target;
   separately record physical reboot, network/resource pressure,
   credential/control-plane, active-stream continuity, and alert delivery.
8. Install only the published artifacts on a clean Ubuntu 24.04 amd64 VM, reboot it, and record authenticated OME plus real ingest/playback evidence before announcing production readiness.
9. Bind all eight required gate artifacts to the same candidate manifest hash
   in a reviewed `docs/releases/promotions/` record. Dispatch stable promotion
   only after the live issues are closed; keep `publish_latest` false unless
   moving convenience aliases is an explicit rollout decision.

## Planned stable title

`BitRiver Live v1.2.3 - self-hosted single-host release`
